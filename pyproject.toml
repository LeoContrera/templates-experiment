[project]
name = "python-proyect-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.1",
    "tailwind>=3.1.5b0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.11",
]
